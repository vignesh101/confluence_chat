[project]
name = "Confluence Chat"

# Note:
# Custom history drawer implementation for conversation management.
# Conversations are stored in a local SQLite database (conversations.db).

[ui]
# Keep minimal header buttons since we have custom drawer
header_buttons = ["step_view", "settings"]

# Custom CSS for drawer styling and UI adjustments
custom_css = "/public/styles.css"

# Custom head for loading JavaScript
custom_head = "/public/head.html"
